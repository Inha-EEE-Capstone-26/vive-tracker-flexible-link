# clean_dataset_1link_v2

Full 1-link cleaned dataset used as the source package for the final `v1000_only_random` 50-seed evaluation.

| Source group | Rows |
|---|---:|
| `test-260523-2` | 297 |
| `test-260524-1` | 584 |
| Total | 881 |

The reported 1-link paper metric is evaluated with `split_mode=v1000_only_random`, so the effective final split uses the `test-260524-1` rows inside this package.
