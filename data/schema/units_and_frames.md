# Units and Coordinate Frames

- Position values are expressed in millimeters.
- Joint angles are expressed in degrees.
- The 1-link package uses a vertical-home-relative Vive tracker frame with a fixed axis mapping.
- The 2-link package uses Kabsch alignment into the target model frame.
- Neither package should be described as full robot-base calibrated unless a later calibration artifact is added.
